# Prompting guidelines

Use the template in backend/app/prompts/safe_poc_template.txt as the base for PoC generation.
Always inject RUNNER_TIMEOUT and TARGET_HOSTS into the template.
Keep prompts short and explicit about constraints (no reverse shells, no persistence).
